#!/bin/bash
# Creates a new HTML project based on https://github.com/lucasmazza/html
if [ $# -lt 1 ]
then
  echo "Usage: $(basename $0) NAME"
  exit 1
fi

git clone git@github.com:lucasmazza/html.git $1 && rm -rf $1/{.git,.gitignore,LICENSE,README.md}
