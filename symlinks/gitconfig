[credential]
  helper = osxkeychain

[hub]
  protocol = https

[core]
  excludesfile = ~/.gitignore
  whitespace = trailing-space,space-before-tab
  autocrlf = input
  pager = less -+$LESS -RS

[apply]
  whitespace = fix

[color]
  ui = true
  interactive = true
  status = true
  branch = true
  diff = true

[alias]
  aliases = config --get-regexp alias
  ci = commit
  co = checkout
  s  = status
  a  = add
  d  = diff
  dc = diff --cached
  rank = shortlog -sn --no-merges
  work = !git config user.email lucas.mazza@magnetis.com.br && git config user.signingkey 70971BDF
  fix = commit --amend -C HEAD
  save = !git add -A && git commit -m wip
  undo = reset --soft HEAD^
  m = merge --no-edit
  autorebase=!GIT_SEQUENCE_EDITOR=touch git rebase --interactive --autosquash

[user]
  email = lucastmazza@gmail.com
  name = Lucas Mazza
  signingkey = A0ED7A3E

[push]
  default = simple

[heroku]
  account = personal

[merge]
  ff = false

[pull]
  rebase = true

[branch]
  autosetuprebase = always

[commit]
  gpgsign = true

[rebase]
  autosquash = true

[github]
  user = lucasmazza

[gpg]
  program = /usr/local/bin/gpg
[protocol]
	version = 2
